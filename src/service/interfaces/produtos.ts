export interface Produto {
    id?: number | any;
    name: string;
    description: string;
    price: number
    quantity: number 
    code: number | string
    fornecedor: string
    added: string | any
}